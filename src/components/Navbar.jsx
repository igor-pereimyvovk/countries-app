import NavbarWrapper from "./UI/NavbarWrapper";
import Logo from "./UI/Logo";

const Navbar = () => {
    return (
        <NavbarWrapper>
            <Logo />
        </NavbarWrapper>
    );
};

export default Navbar;
